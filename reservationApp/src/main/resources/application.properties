spring.application.name=reservationApp
# Configuration de la base de données
spring.datasource.url=jdbc:mysql://localhost:3306/reservationdb
spring.datasource.username=root  # Remplace par ton utilisateur MySQL
spring.datasource.password=ton_mot_de_passe
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update  # Crée les tables automatiquement si elles n'existent pas
